var Ball = function(ballType) {
   // construct a new Ball object
   // The type of ball optional, defaults to "regular"
  // If an argument is provided, use it; otherwise, default to "regular"
    this.ballType = ballType || "regular";
};