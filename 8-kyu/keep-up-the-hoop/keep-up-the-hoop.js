function hoopCount (n) {
  // use ternary operation and return 
  // if n is greater than or equal to ten, "Greet..."
  // else "Keep at it..."
   return n >= 10 
    ? "Great, now move on to tricks"
    : "Keep at it until you get it"
}