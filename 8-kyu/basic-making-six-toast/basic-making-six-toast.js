function sixToast(num) {
  // return absolute value
  // if num is less than 6
  return Math.abs(num < 6  
  // subtract num from 6
                  ? 6 - num
  // else if num is more than 6 
                  : num > 6
  // subtract 6 from num
                  ? num - 6
  // else if num is equal to 6 
  // 0
                  : 0
                  )
}