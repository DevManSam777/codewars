function solution(str){
  // use the split method to convert to an array
  // use the reverse method to reverse the characters with the array
  // use the join method to convert back into a string and return
  return str.split('').reverse().join('');
}