function makeNegative(num) {
  // return
  // ternary operation: condition ? if true : false
  // num greater than 0
  // num is positive multiply it by -1 to make it negative
  // num is negative return num
  return num > 0 ? num * -1 : num
}